[package]
name = "hydrogen_src"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "*", default-features = false, features = ["blocking", "rustls-tls"] }
thiserror = "*"
wavers = "*"
zip = "*"
